/* Webpack won't build if there's not at least one JSX entry below the site folder.
   This placeholder takes care of that.
   Remove me when you add your own React components below site - primarily below /parts or /pages.
 */

export default {};
